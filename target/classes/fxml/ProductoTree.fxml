<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>
<BorderPane
        xmlns="http://javafx.com/javafx"
        xmlns:fx="http://javafx.com/fxml"
        fx:controller="SORT_PROYECTS.AppInventario.controller.ProductoTreeController"
        style="-fx-padding: 0;"
        VBox.vgrow="ALWAYS"
        HBox.hgrow="ALWAYS">

    <!-- Panel izquierdo: búsqueda -->
    <left>
        <VBox fx:id="panelLateral" spacing="15" alignment="TOP_LEFT" styleClass="sidebar" prefWidth="200">
            <padding>
                <Insets top="20" right="20" bottom="20" left="20"/>
            </padding>

            <Label text="Busqueda"/>
            <TextField fx:id="txtBuscarEtiqueta" promptText="Buscar..."/>

            <Label text="Buscar por:"/>

            <VBox spacing="10" alignment="CENTER">
                <ToggleButton fx:id="btnNombre" text="Nombre" onAction="#buscarPorNombre"/>
                <ToggleButton fx:id="btnCategoria" text="Categoría" onAction="#buscarPorCategoria"/>
                <ToggleButton fx:id="btnEtiqueta" text="Etiqueta" onAction="#buscarPorEtiqueta"/>

            </VBox>

            <VBox spacing="10" style="-fx-padding: 30 0 0 0;">
                <Label text="Manejo de datos"/>
            </VBox>

            <VBox spacing="10" alignment="CENTER">
                <Button text="➕ Agregar producto" onAction="#abrirAgregarProducto"/>
                <Button text="➕ Agregar categoría" onAction="#agregarCategoria"/>
                <Button text="+ Agregar variante" onAction="#agregarVariante"/>
                <Button text="🗑 Eliminar producto" onAction="#eliminarProducto"/>
            </VBox>
        </VBox>
    </left>
    <!-- Centro: Tabla -->
    <center>
        <VBox spacing="10" style="-fx-padding: 0 20 0 0;">
            <TreeTableView fx:id="tablaInventarioTree" prefHeight="600.0" VBox.vgrow="ALWAYS" />
        </VBox>
    </center>
    <!-- Inferior: barra de acciones -->
    <bottom>
        <HBox spacing="15" alignment="CENTER" styleClass="footer">
            <padding>
                <Insets top="15" right="15" bottom="15" left="15"/>
            </padding>

            <Button fx:id="btnNuevaVenta" text="Nueva venta" onAction="#iniciarVenta"/>
            <Button fx:id="btnStockBajo" text="Stock bajo" onAction="#mostrarBajoStock"/>
            <Button fx:id="btnExportarPDF" text="📄 Exportar PDF" onAction="#exportarVistaPDF"/>
        </HBox>
    </bottom>



</BorderPane>
